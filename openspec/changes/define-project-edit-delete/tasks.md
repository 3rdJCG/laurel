## 1. プロジェクトカードの操作UIの準備

- [ ] 1.1 `src/renderer/src/screens/HomeScreen.tsx` のプロジェクトカードにホバー時に表示される編集ボタン（鉛筆アイコン）と削除ボタン（ゴミ箱アイコン）を追加する
- [ ] 1.2 `editingProjectId` state を `HomeScreen.tsx` に追加し、同時に1件のみ編集モードになるよう管理する

## 2. プロジェクト名インライン編集の実装

- [ ] 2.1 `editingProjectId` が対象プロジェクトと一致する場合、カード内のプロジェクト名を入力フィールドに切り替えて表示する
- [ ] 2.2 Enter キーで確定、Escape キーでキャンセルできるキーハンドラーを実装する
- [ ] 2.3 空のプロジェクト名で確定しようとした場合に変更を拒否するバリデーションを実装する
- [ ] 2.4 編集中はカードのナビゲーション（プロジェクト画面への遷移）を無効化する
- [ ] 2.5 確定時に Project の `name` を更新し、`data:save-project` IPC で永続化する

## 3. プロジェクト削除UIの実装

- [ ] 3.1 削除ボタン押下でカード内にインライン確認UI（確認メッセージ・「削除」・「キャンセル」ボタン）を表示する
- [ ] 3.2 確認UIの確認メッセージに、対象プロジェクトに属するタスク件数を含める
- [ ] 3.3 「削除」確定時に対象プロジェクトおよび紐付くすべてのタスクを state から除去する
- [ ] 3.4 `data:delete-project` IPC を呼び出してプロジェクトファイルを削除する
- [ ] 3.5 「キャンセル」押下で確認UIを閉じ、カード通常表示に戻る

## 4. 動作確認

- [ ] 4.1 `npm run dev` で起動し、プロジェクト名のインライン編集（確定・キャンセル・空バリデーション）が動作することを確認する
- [ ] 4.2 プロジェクト削除時にタスク件数が確認UIに表示され、削除後にプロジェクトとタスクが消えることを確認する
- [ ] 4.3 タスクが0件のプロジェクトも削除できることを確認する
- [ ] 4.4 `npm run typecheck` で型エラーがないことを確認する