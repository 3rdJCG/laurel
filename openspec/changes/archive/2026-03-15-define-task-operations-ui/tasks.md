## 1. TaskItem コンポーネントの作成

- [x] 1.1 `src/renderer/src/components/TaskItem.tsx` を作成し、タスクの表示・操作UIを担うコンポーネントを定義する
- [x] 1.2 `editingTaskId` state を `ProjectScreen.tsx` に追加し、同時編集を1件に制限する仕組みを実装する
- [x] 1.3 `depth` props（数値）を TaskItem に追加し、階層の深さをインデント幅に反映させる

## 2. ステータス変更UIの実装

- [x] 2.1 TaskItem にステータスバッジを実装する（`todo` / `in-progress` / `in-review` / `done` を色・ラベルで識別）
- [x] 2.2 ステータスバッジのクリックでドロップダウン選択UIを展開する動作を実装する
- [x] 2.3 ステータス選択時に Task の `status` を更新し、`data:save-project` IPC で永続化する

## 3. タスク編集UIの実装

- [x] 3.1 TaskItem に編集ボタンを追加し、押下で編集モードに切り替わる動作を実装する
- [x] 3.2 編集モードでタイトル入力フィールドを表示し、Enter / Escapeキーで確定 / キャンセルできるよう実装する
- [x] 3.3 空タイトルでの確定を拒否するバリデーションを実装する
- [x] 3.4 編集モードで genre 入力フィールドを表示し、空入力で `null` になるよう実装する
- [x] 3.5 編集モードで tags 入力UIを実装する（タグの追加・個別削除ができること）
- [x] 3.6 編集確定時に Task を更新し、`data:save-project` IPC で永続化する

## 4. タスク削除UIの実装

- [x] 4.1 TaskItem に削除ボタンを追加し、押下でインライン確認UI（「削除」「キャンセル」ボタン）を表示する
- [x] 4.2 親タスクの場合は確認UIに子タスクも削除される旨の警告を追加する
- [x] 4.3 削除確定時に対象タスクとすべての子孫タスクを state から除去し、`data:save-project` IPC で永続化する

## 5. サブタスク作成UIの実装

- [x] 5.1 TaskItem に「サブタスクを追加」ボタンを実装する（`depth >= 5` の場合は非表示）
- [x] 5.2 ボタン押下でタスクアイテム直下にサブタスクタイトル入力フォームを表示する動作を実装する
- [x] 5.3 サブタスク作成時に `parentId` を親タスクの `id` に設定し、`genre` / `tags` に親の値を初期値として設定する
- [x] 5.4 作成されたサブタスクを親タスクの直下にインデント付きで表示する

## 6. ProjectScreen への組み込みと動作確認

- [x] 6.1 `ProjectScreen.tsx` で TaskItem コンポーネントを使用し、タスクツリーを再帰的にレンダリングする
- [x] 6.2 `npm run dev` で起動し、ステータス変更・編集・削除・サブタスク追加の各操作が動作することを確認する
- [x] 6.3 5層制限（サブタスク追加ボタン非表示）が正しく動作することを確認する
- [x] 6.4 `npm run typecheck` で型エラーがないことを確認する
