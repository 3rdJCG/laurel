## 1. ジャンル永続化（settings IPC 拡張）

- [x] 1.1 `settings:get` / `settings:set` で `genres: string[]` キーを扱えることを確認する（既存 IPC の動作確認）
- [x] 1.2 Main プロセス側でジャンル一覧の初期値（空配列）をデフォルト設定に追加する

## 2. ジャンル管理 UI（設定画面への追加）

- [x] 2.1 設定モーダル内に「ジャンル管理」セクションを追加する
- [x] 2.2 登録済みジャンルを一覧表示するコンポーネントを実装する
- [x] 2.3 ジャンル名入力フィールドと追加ボタンを実装する（空文字・重複バリデーション含む）
- [x] 2.4 ジャンル削除ボタンを実装する（削除時の警告ダイアログを含む）
- [x] 2.5 警告ダイアログで該当タスク件数を表示するロジックを実装する

## 3. タグサジェスト（タスクフォーム拡張）

- [x] 3.1 プロジェクト内全タスクの `tags` を収集・dedup するユーティリティ関数を実装する
- [x] 3.2 タグ入力フィールドにオートコンプリートコンポーネントを組み込む
- [x] 3.3 入力文字列でのサジェスト候補絞り込みを実装する（前方一致 or 部分一致）
- [x] 3.4 既に追加済みのタグを候補から除外する処理を実装する
- [x] 3.5 候補にないタグを自由入力で追加できることを確認する

## 4. ジャンル・タグフィルター（プロジェクト画面）

- [x] 4.1 プロジェクト画面にフィルターバーコンポーネントを追加する
- [x] 4.2 ジャンルドロップダウンを実装する（登録済みジャンル一覧 + 「すべて」選択肢）
- [x] 4.3 タグマルチセレクトを実装する（プロジェクト内使用済みタグ一覧）
- [x] 4.4 選択中のジャンル・タグに基づくタスクフィルタリングロジックを実装する（AND条件）
- [x] 4.5 フィルター適用後にタスクが0件の場合の空状態メッセージを実装する
- [x] 4.6 画面遷移時にフィルター状態がリセットされることを確認する

## 5. 動作確認

- [x] 5.1 ジャンルを登録・一覧表示・削除し、アプリ再起動後に永続化されていることを確認する
- [x] 5.2 タスク作成フォームでタグサジェストが表示され、選択・自由入力できることを確認する
- [x] 5.3 フィルターバーでジャンル単体・タグ単体・両者の組み合わせ絞り込みが動作することを確認する