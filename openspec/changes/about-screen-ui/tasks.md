## 1. バージョン取得 IPC 追加

- [x] 1.1 メインプロセスに `app:get-version` IPC ハンドラーを追加する（`app.getVersion()` を返す）
- [x] 1.2 preload の contextBridge に `app:get-version` を追加する

## 2. About モーダルコンポーネント実装

- [x] 2.1 `AboutModal` コンポーネントを作成する
- [x] 2.2 `app:get-version` IPC を呼び出してバージョンを取得し表示する
- [x] 2.3 アプリ名（Laurel）・バージョン・MIT ライセンス表記・著作権表示を表示する
- [x] 2.4 プライバシー情報（「外部にデータを送信しない」旨）を表示する
- [x] 2.5 閉じるボタンと背景クリックでモーダルを閉じる処理を実装する

## 3. ナビゲーションへの導線追加

- [x] 3.1 ナビゲーションバーに About リンクまたはアイコンボタンを追加する（サイドバー経由）
- [x] 3.2 About リンクをクリックすると `AboutModal` が表示されるようにする

## 4. 動作確認

- [ ] 4.1 ナビゲーションの About リンクをクリックしてモーダルが開くことを確認する
- [ ] 4.2 モーダルにアプリ名・バージョン・ライセンス・プライバシー情報が表示されることを確認する
- [ ] 4.3 モーダルを閉じると元の画面に戻ることを確認する
