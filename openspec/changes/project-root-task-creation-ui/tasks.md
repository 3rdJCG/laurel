## 1. UI コンポーネント実装

- [ ] 1.1 ProjectScreen にルートタスク追加ボタン（「＋ タスクを追加」）を追加する
- [ ] 1.2 タスク追加ボタン押下でインラインタイトル入力フォームを展開する state を実装する
- [ ] 1.3 タイトル入力フォームに Enter キーで確定、Escape キーでキャンセルのキーボードハンドラを追加する
- [ ] 1.4 タイトルが空のまま確定操作をしてもタスクが作成されないバリデーションを追加する

## 2. 空状態 UI

- [ ] 2.1 タスクが0件のときに空状態メッセージと「＋ タスクを追加」ボタンを表示する

## 3. データ保存

- [ ] 3.1 ルートタスク作成時に `parentId` を付与せず Task を生成し、プロジェクトの tasks 配列に追加する
- [ ] 3.2 タスク追加後に `data:save-project` IPC を呼び出してデータを永続化する