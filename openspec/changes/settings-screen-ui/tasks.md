## 1. IPC ブリッジの追加（フォルダ選択ダイアログ）

- [ ] 1.1 `src/main/index.ts` に `dialog:open-folder` IPC ハンドラーを追加する（`dialog.showOpenDialog({ properties: ['openDirectory'] })` を呼び出し、キャンセル時は `null`、選択時は選択パスを返す）
- [ ] 1.2 `src/preload/index.ts` に `dialog:open-folder` の contextBridge API を追加する

## 2. SettingsModal コンポーネントの実装

- [ ] 2.1 `src/renderer/src/components/SettingsModal.tsx` を作成する（モーダル骨格・開閉制御の props）
- [ ] 2.2 モーダル表示時に `settings:get` を呼び出し、現在の保存先パスを取得して表示する
- [ ] 2.3 保存先パスを表示・編集できるテキストフィールドを追加する（UNC パスの手入力にも対応）
- [ ] 2.4 「フォルダを選択」ボタンを追加する（クリック時に `dialog:open-folder` を呼び出してテキストフィールドに反映する）
- [ ] 2.5 「変更を保存」ボタンを追加する（クリック時に `settings:set` を呼び出す）
- [ ] 2.6 `settings:set` がエラーを返した場合、モーダル内にエラーメッセージをインライン表示し、`settings:get` を再度呼んで表示を更新する
- [ ] 2.7 「キャンセル」ボタンを追加する（クリック時にモーダルを閉じ、変更を破棄する）

## 3. メイン画面への統合

- [ ] 3.1 `src/renderer/src/App.tsx` に設定モーダルの開閉状態（`useState`）を追加する
- [ ] 3.2 `App.tsx` に設定ボタン（歯車アイコン等）を追加し、クリックでモーダルを開く
- [ ] 3.3 `SettingsModal` を `App.tsx` に組み込み、開閉状態を渡す

## 4. 動作確認

- [ ] 4.1 設定ボタンをクリックして設定モーダルが開き、現在の保存先が表示されることを確認する
- [ ] 4.2 「フォルダを選択」でOS ダイアログが開き、選択したパスがフィールドに反映されることを確認する
- [ ] 4.3 UNC パスをテキストフィールドに手入力して保存できることを確認する
- [ ] 4.4 存在しない/アクセス不可のパスを指定したときにエラーメッセージが表示されモーダルが閉じないことを確認する
- [ ] 4.5 キャンセルボタンで設定が保存されずにモーダルが閉じることを確認する
- [ ] 4.6 `npm run typecheck` でエラーがないことを確認する