## 1. データモデル拡張（order フィールド追加）

- [x] 1.1 タスクの TypeScript 型定義に `order: number` フィールドを追加する
- [x] 1.2 `data:load-all` のレスポンス処理で `order` が存在しないタスクに配列インデックスを補完するマイグレーション処理を追加する
- [x] 1.3 タスク作成時に同一階層の末尾 `order` 値を割り当てる処理を追加する

## 2. 展開・折りたたみ実装

- [x] 2.1 `@dnd-kit/core` と `@dnd-kit/sortable` を依存に追加する（`npm install @dnd-kit/core @dnd-kit/sortable`）
- [x] 2.2 プロジェクト画面に展開状態を管理する state（`Set<taskId>`）を追加する
- [x] 2.3 子タスクを持つタスクアイテムに展開・折りたたみトグルアイコンを追加する
- [x] 2.4 トグルクリック時に展開状態を切り替えるハンドラーを実装する
- [x] 2.5 展開状態に応じて子タスクの表示・非表示を制御するレンダリングロジックを実装する
- [x] 2.6 プロジェクト画面初期化時にすべてのタスクが折りたたみ状態になることを確認する

## 3. ドラッグ＆ドロップ並び替え実装

- [x] 3.1 `@dnd-kit/sortable` を使って同一階層のタスクリストを SortableContext でラップする
- [x] 3.2 各タスクアイテムにドラッグハンドルを追加する
- [x] 3.3 ドロップ完了時（`onDragEnd`）に同一階層内の `order` 値を更新する処理を実装する
- [x] 3.4 異なる親への移動をブロックする処理を実装する（同一 `parentId` チェック）
- [x] 3.5 `order` 更新後に `data:save-project` を呼び出して永続化する

## 4. 動作確認

- [x] 4.1 子タスクを持つタスクの展開・折りたたみが正しく動作することを確認する
- [x] 4.2 同階層内でのドラッグ＆ドロップ並び替えが動作し、再起動後も順序が保持されることを確認する
- [x] 4.3 既存タスクデータに `order` フィールドがない場合の読み込みが正常に動作することを確認する